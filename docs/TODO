### TODO 리스트
- 페이징 처리 테스트 코드 실패
- SpringBoot 전체를 띄우는 @IntegrationTEST의 경우 local database를 사용하다보면 기존에 데이터가 존재해서 성공 테스트가 실패
   - 그래서 보통은 docker을 활용해 데이터베이스 container을 띄워서 테스트를 수행
- 게시글을 수정 했다면, 스터디 그룹 정보를 가져오지 못함.
- 요청 받은 친구 목록이 모든 유저 공유되고 있음
- 자신의 프로필에서 '친구 요청' 클릭하면 요청이 가능하게 되어 있음
- 친구 요청을 보내고 바로 출력되지 않음
- 친구 "수락"을 받고 '친구 목록'에 출력되는걸 확인했는데, 로그아웃하고 보면 사라져 있음
- 친구 "수락"을 받고 친구 요청을 보낸 유저의 계정으로 들어가면 '친구 목록'에 표시가 되어 있지 않음
